{% extends 'base.jinja' %}

{% block content %}
    <h2>Car</h2>
    <form action="" method="post">
        {{ car_form.hidden_tag() }}
        <p>
        {{ car_form.post.label }} <br>
        {{ car_form.post.(size=32) }}
        </p>
        <p>
        {{ car_form.submit(class='btn btn-primary') }}
        </p>
    

    </form>
{% endblock %}